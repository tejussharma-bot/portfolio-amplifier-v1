# Behance

Product mode: export-first

Auth URL: none in V1
Token URL: none in V1
API base URL: none in V1
Required scopes: none in V1
Required headers: none in V1

Required env vars:
- none

Redirect URI rules:
- not applicable in V1 export mode

Supported actions:
- generate a Behance-ready export pack
- copy long-form structure to clipboard
- open the Behance new-project flow

Unsupported actions:
- direct posting
- scheduling

Fallback behavior:
- Behance must be labeled `Export only`.
- The UI must not imply direct posting until a verified Adobe/Behance path exists and is tested.
